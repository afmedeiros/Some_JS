//Método MAP - filtro nos elementos conforme o desejado (pode ser multiplicar, pode ser dividir, etc)

let inteiros = [2,4,6,9]

inteiros.map(Resto) //Map sempre precisa de uma function para dizer o que ela vai fazer

function Resto(num){

    return num%2
}